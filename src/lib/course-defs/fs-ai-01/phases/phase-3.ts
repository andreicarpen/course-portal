import type { PhaseMetadata } from '@/lib/types';

export const phaseMetadata: PhaseMetadata = {
  id: 'phase-3',
  title: 'Phase 3: Frontend Development', // Placeholder title - update later
  moduleIds: [] // Placeholder module IDs - update later
}; 